import * as React from "react";
import styled from "styled-components";
import Fullscreen from "./Fullscreen";

const Center = styled(Fullscreen)`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
`;

export default Center;
